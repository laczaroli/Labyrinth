package jfx;

/**
 * Neccessary a launcher like this if we want to run a JavaFX
 * application.
 */
public class Launcher {
    /**
     * The main class that calls the official main.
     * class.
     * @param args main arguments
     */
    public static void main(String[] args) {
        Main.main(args);
    }
}
